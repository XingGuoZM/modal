/**
 * 弹窗类型枚举
 */
const popTypeEnum = {
  modal: 'modal',
  popup: 'popup',
  popdown: 'popdown',
  popleft: 'popleft',
  popright: 'popright',
};

/**
 * 弹窗显示方式的枚举
 */
const showMethodEnum = {
  // 覆盖
  cover: 'cover',
  // 共存
  coexist: 'coexist',
};

export { popTypeEnum, showMethodEnum };
